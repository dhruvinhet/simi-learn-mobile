# Configuration is split by trust boundary.
# Copy apps/mobile/.env.example to apps/mobile/.env for public mobile configuration.
# Copy supabase/.env.example to supabase/.env for server-only deployment secrets.
# Never put Groq keys, Supabase secret/service-role keys, or webhook tokens in apps/mobile.
